# Normalise line endings in the repository; keep the shell recipe LF on every
# platform, since a CRLF `labels.sh` does not run under `sh`.
* text=auto
*.sh text eol=lf

# Brand assets are not the documentation; keep them out of the language bar.
brand/** linguist-vendored
