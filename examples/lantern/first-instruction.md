# The generated first instruction for #118

*This is the positional prompt the launcher passed to the harness — the
seat's first turn, built from the issue rather than composed. It is
reproduced as the seat received it; only the verbatim issue sections are
abbreviated with `…` where they repeat `mission-issue.md`.*

```
You are executing mission #118 of the Lantern repository, under the Guild role 'claude-code'. This instruction was generated by tools/mission_launcher.py from the issue itself; it is not a summary and it does not replace the issue.

Read the issue in full first: https://github.com/lantern-workshop/lantern/issues/118
It carries 2 comment(s). A comment may amend the prose sections, and the body alone can start you on a false premise, so read every one:
  gh issue view 118 --repo lantern-workshop/lantern --json comments --jq '.comments[] | .url, .body'

--- the contract, as the form declares it ---
  Agent: Claude Code
  Capabilities required: shell: host-native, windows-desktop: yes
  Guild role: claude-code
  Model: claude-opus-5
  Effort: HIGH
  Budget: MEDIUM
  Mode: one writer
  Risk: MEDIUM
  Concurrency: within standing ceiling
  Subagents: forbidden (default)
  Human gate: routine — delegated merge (Product Owner standing ruling, recorded on the delegation issue)
  Report target: Chief Architect, as a comment on this issue
  Branch: feature/dashboard-stale-reading
  Worktree: C:\repos\lantern-worktrees\wt-dashboard-stale-reading
  Governance: 3f9c2d1a7b8e4c05d6f1a2b3c4d5e6f708192a3b (origin/develop, fetched at launch)

Condition 6 stood over every dropdown above and every field a condition addresses by name; it found no comment setting one. It did not stand over: Report target. A comment that names a value the body already declares is not an amendment and was not refused.

--- Objective (verbatim from the issue) ---
Deliver the smallest lot that keeps a Dashboard tile showing its **last reading, marked stale with the time it was taken**, from the moment the bench-controller bridge drops until it reconnects — instead of blanking the tile to `—` and losing the reading and its age. A stale reading is never presented as live.

--- Acceptance criteria (verbatim from the issue) ---
1. When `BridgeState` transitions to `DISCONNECTED`, every tile that held a reading keeps it, …
…
6. `docs/changelog.d/118-dashboard-stale-reading.md` exists and describes the change in the words a release note would use.

--- Scope (verbatim from the issue) ---
…

--- Out of scope (verbatim from the issue) ---
…

--- Stop conditions (verbatim from the issue) ---
…

--- Invariants (verbatim from the issue) ---
- [x] One writer respected — this mission does not create a second writer.
…

--- session entry, already performed for you ---
  $env:PYTHONPATH = 'C:\repos\lantern-worktrees\wt-dashboard-stale-reading\src'; C:\repos\lantern\.venv\Scripts\python.exe -m lantern guild-hi --role claude-code
Do not run guild-hi again: a second run overwrites the entry snapshot guild-bye compares against, and reports a stale-snapshot warning for a session that never happened. Close with exactly one guild-bye. The transcript follows.

Guild session entry — Lead Software Engineer (claude-code)
  branch feature/dashboard-stale-reading @ 3f9c2d1 (tracking: none yet — push empty before the first file is written)
  working tree: clean
  open pull requests on this branch: none

Operational state — source: docs/guild/journal/20260902T174420Z-claude-code-8b1e0f3.md
  [INFO] not branch-local — latest entry overall; it describes another line of work, and rank 2 (branch and PR state as observed) governs where they disagree
  work state: complete
  milestone: sync export retry — delivered, PR #121 merged
  smallest next action: none carried — the entry declares itself finished
  next decision owner: Chief Architect

Curated handover — docs/guild/HANDOVER.md (revision 2026-08-30)
  priority: Dashboard resilience while the bridge is unstable
  next epic: Device page calibration panel

stale snapshot: none found — no previous session left a marker in this worktree

Preservation census (this machine, 5 registered worktrees):
  unpreserved: none
  unknown: wt-sync-export-retry — HEAD reflog names 1 commit no remote ref contains; could not establish whether it is load-bearing
  (preserved count omitted from this relay by rule)

Role: Lead Software Engineer — domain: implementation, integration, refactoring, tests, branches/commits/PRs, technical documentation, adversarial self-review
  escalate to: Chief Architect (architecture/vision conflicts), Domain Expert (measurement and calibration policy), Product Owner (merge, product arbitration)
  mandatory reading: docs/guild/HANDOVER.md, AI_TEAM.md, CLAUDE.md
guild-hi exit 0

Post the transmission report to the Report target above before guild-bye.
**Name your execution parameters in it**, in the form the pull request template asks for: model `claude-opus-5`, effort `HIGH`, started by `tools/mission_launcher.py`. This line is what makes a substitution visible in a durable artefact instead of in somebody's scrollback -- state what actually ran, and say so plainly if it is not what this instruction names.
```

Three properties of this text are worth noticing. **It names the
governance commit** the contract was measured against, so the seat and any
later reader can say which revision of the rules governed. **It reproduces
the prose sections verbatim** rather than summarising them — a summary is
where a criterion gets lost. And **it carries the entry transcript**,
including the preservation census and the stale-snapshot line the seat must relay,
so the evidence the transmission report must cite is already in the seat's
hands.
